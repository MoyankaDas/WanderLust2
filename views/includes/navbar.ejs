<style>
  body {
    background-color: #f8f9fa;
    color: #212529;
    transition: background-color 0.3s, color 0.3s;
  }

  .dark-mode {
    background-color: #121212 !important;
    color: #f1f1f1 !important;
  }

  .dark-mode .navbar {
    background-color: #1e1e1e !important;
    border-color: #333 !important;
  }

  .dark-mode .nav-link,
  .dark-mode .navbar-brand,
  .dark-mode .form-control {
    color: #f1f1f1 !important;
  }

  .dark-mode .search-btn {
    background-color: #bb86fc !important;
  }

  .theme-toggle-btn {
    border: none;
    background: none;
    font-size: 1.4rem;
    margin-left: 1rem;
    cursor: pointer;
  }

  .search-btn {
    background-color: #fe4264;
    color: white;
    border-radius: 25px;
    padding: 0 1rem;
  }

  .search-btn:hover {
    background-color: #fe4264;
    color: white;
  }

  .search-btn i {
    display: inline;
    margin-right: 0.5rem;
  }

  .search-inp {
    border-radius: 25px;
    padding: 0.5rem 3rem;
  }

  #searchBar {
    color: black !important;
  }
</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings"><b>Explore</b></a>
      </div>

     

          <% if (currUser && currUser.isAdmin) { %>
            <div class="nav-item">
              <a class="nav-link" href="/balance-sheet"><b>📊 Balance Sheet</b></a>
            </div>
            <% } %>

              <!-- Search form -->
              <div class="navbar-nav ms-auto">
                <form class="d-flex" role="search" onsubmit="return false;">
                  <input id="searchBar" class="form-control me-2 search-inp" type="search"
                    placeholder="Search by country" aria-label="Search" />
                  <button class="btn search-btn" type="button">
                    <i class="fa-solid fa-magnifying-glass"></i>Search
                  </button>
                </form>
              </div>



              <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/listings/new">
                  <i class="fas fa-house-user me-1 text-primary"></i><b> WanderLust your home</b>
                </a>

                <% if (!currUser) { %>
                  <a class="nav-link" href="/signup"><i class="fas fa-user-plus me-1 text-success"></i><b>Sign
                      Up</b></a>
                  <a class="nav-link" href="/login"><i class="fas fa-sign-in-alt me-1 text-warning"></i><b>Log
                      In</b></a>
                  <% } %>

                    <% if (currUser) { %>
                      <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt me-1 text-danger"></i><b>Log
                          Out</b></a>
                      <a class="nav-link" href="/users/bookings"><i
                          class="fas fa-calendar-check me-1 text-info"></i><b>My Bookings</b></a>
                      <% } %>

                        <!-- Dark Mode Toggle Button -->
                        <button class="theme-toggle-btn nav-link" id="themeToggleBtn"
                          title="Toggle dark mode">🌓</button>
              </div>
    </div>
  </div>
</nav>


<!-- ////////////////////// -->
<script>
  // Load saved theme on page load
  document.addEventListener('DOMContentLoaded', () => {
    if (localStorage.getItem('theme') === 'dark') {
      document.body.classList.add('dark-mode');
    }

    document.getElementById('themeToggleBtn').addEventListener('click', () => {
      document.body.classList.toggle('dark-mode');
      const theme = document.body.classList.contains('dark-mode') ? 'dark' : 'light';
      localStorage.setItem('theme', theme);
    });
  });
</script>